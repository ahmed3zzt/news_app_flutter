class ArticleModel {
  final String? image, title, description, url;
  const ArticleModel({
    required this.image,
    required this.title,
    required this.description,
    required this.url,
  });
}
